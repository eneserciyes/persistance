# Application configuration
server.port=8091
spring.application.name=persistence

## MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ku-calendar
spring.datasource.username=root
spring.datasource.password=merciyes.220918

#`hibernate_sequence' doesn't exist
spring.jpa.hibernate.use-new-id-generator-mappings=false
spring.jpa.hibernate.ddl-auto = create

##Turn Statistics on and log SQL stmts
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

##If want to see very extensive logging
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.type=trace
logging.level.org.hibernate.stat=debug

# Cache Properties
cache.instanceName=hazelcast-instance
cache.configuration.name=authenticationCache
